# ─────────────────────────────────────────────────────────────────────────────
# Environment Variables — copy this file to .env before running Docker
#
#   cp .env.example .env
#
# ⚠️  NEVER commit .env to version control.
# ⚠️  Generate all secrets before deploying to production:
#       openssl rand -hex 32   →  for passwords
#       openssl rand -hex 64   →  for JWT_SECRET
# ─────────────────────────────────────────────────────────────────────────────

# ── MySQL ─────────────────────────────────────────────────────────────────────
# 🔴 CHANGE THESE before deploying! Run: openssl rand -hex 32
MYSQL_ROOT_PASSWORD=REPLACE_WITH_STRONG_ROOT_PASSWORD
DB_HOST=mysql
DB_USER=ecomuser
DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
DB_NAME=ecommerce

# ── JWT ───────────────────────────────────────────────────────────────────────
# 🔴 MUST change! Run: openssl rand -hex 64
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_HEX_STRING

# ── CORS ─────────────────────────────────────────────────────────────────────
# Set to your production frontend URL (comma-separated for multiple origins)
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=http://localhost

# ── Elasticsearch ─────────────────────────────────────────────────────────────
ELASTICSEARCH_URL=http://elasticsearch:9200
